<!--User avatar & name & badge-->
<%if(recipe&&recipe.user){%>
<div class="recipe">
  <div class="user">
    <img src="<%= recipe.user.avatar %>" class="user" />
    <%= recipe.user.userName %>
    <!--Display badge of honor if Expert user-->
    <%if (recipe.user.role.toLowerCase()=="expert"){%> <img src="images/badge_of_honor.png" class="badge" /><%}%>
  </div>
</div>
<%}%>
